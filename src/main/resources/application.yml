spring:
  application:
    name: reconService

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true

recon:
  processing:
    near-real-time-window-seconds: 60
    max-batch-size: 1000
