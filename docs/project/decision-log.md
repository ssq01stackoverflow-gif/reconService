# 设计决策索引

本文档记录已经确认的重要项目决策。详细说明可以放在对应主题文档中，本文档负责提供索引和简短结论。

## 记录规则

- 已确认并影响后续实现方式的决策，应记录在这里。
- 待确认决策先放在 `.agents/docs/decisions/`。
- 决策完成实现并沉淀为项目事实后，再同步到 `docs/`。
- 如果决策被替换，应保留历史记录，并说明替代决策。

## 已确认决策

| 日期 | 决策 | 状态 | 说明 |
| --- | --- | --- | --- |
| 2026-05-25 | 使用 Java 8 和 Spring Boot 2.7.x | 已确认 | 保持 Java 8 运行兼容性，不升级到 Spring Boot 3.x，除非同时升级到 Java 17+。 |
| 2026-05-25 | 不引入 Lombok | 已确认 | 避免 Java 8、IDE 和 javac 兼容问题。 |
| 2026-05-25 | 当前阶段采用分层单体 DDD 结构 | 已确认 | 先保持清晰边界，不提前拆分微服务。 |
| 2026-05-25 | `domain` 不依赖 Spring、HTTP、数据库、MQ、DTO 或 infrastructure 实现 | 已确认 | 保持领域层纯净，便于测试和演进。 |
| 2026-05-25 | `.agents/docs/` 作为草稿区，`docs/` 作为项目事实区 | 已确认 | 支持先自然语言推演，再实现，最后沉淀事实文档。 |
| 2026-05-25 | 使用 `docs/project/` 记录项目进展和项目变更 | 已确认 | 避免项目增长后遗忘进展，并提供人类可读的变更摘要。 |
| 2026-05-26 | 任务创建幂等键使用 `eventKey + ruleCode` | 已确认 | 保证同一事件同一规则只建一个任务，同时允许同一业务键在不同事件下多次触发。 |
| 2026-05-26 | `ruleCode + bizType + bizKey` 作为业务聚合键 | 已确认 | 用于查询、聚合和人工排查，不作为任务创建唯一键。 |
| 2026-05-26 | 第一版保存完整 `rawMessage` | 已确认 | 优先保证排查能力，后续容量压力明显时再考虑压缩或归档。 |
| 2026-05-26 | 告警成功后任务保持 `ALERTING` | 已确认 | 人工确认需要处理后进入 `PAUSED`，人工确认噪音后进入 `RESOLVED`。 |

## 待提升决策

暂无。
