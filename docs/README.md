# 项目文档

`docs` 用于承载已经确认、已经实现或明确采纳的项目事实。

如果文档内容与代码不一致，应优先修正文档或代码，让两者重新对齐。需求草案、方案探索和测试用例规划应先写入 `.agents/docs/`，确认并实现后再提升到本目录。

## 目录

- `architecture/`：架构分层、包结构和依赖规则。
- `domain/`：领域模型、领域命名和核心业务规则。
- `api/`：API 设计原则和接口约定。
- `development/`：开发环境、构建和测试说明。
- `project/`：项目状态、路线图、变更记录和设计决策索引。
